{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.component" (dict "target" "compactor" "component" .Values.compactor "ctx" .) | nindent 4 }}
{{- end }}
