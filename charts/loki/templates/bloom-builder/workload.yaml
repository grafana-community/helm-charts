{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.component" (dict "ctx" . "component" .Values.bloomBuilder "memberlist" false "target" "bloom-builder") }}
{{- end }}
