{{- if and .Values.minio.enabled (not .Values.ignoreMinioDeprecation) }}
{{- fail "The tempo-distributed chart built-in MinIO dependency is deprecated and will be removed in chart v4.0.0 (targeting Tempo 4.x, ~mid-2027). Deploy MinIO externally and configure storage.trace.s3 to point at it. To continue using the built-in subchart in the meantime, set ignoreMinioDeprecation=true." }}
{{- end }}
